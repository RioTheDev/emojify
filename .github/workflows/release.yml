name: Release Build

on:
  push:
    tags:
      - "v*"

  workflow_dispatch:

permissions:
  contents: write

jobs:
  arch-binary-x86_64:
    name: "Build Arch Binary (x86_64)"
    runs-on: self-hosted
    container:
      image: archlinux:base-devel
      options: --privileged
      volumes:
        - /tmp/emojify/pacman-cache-x86_64:/var/cache/pacman/pkg
    steps:
      - uses: actions/checkout@v6
      - name: Update System and Install Deps
        run: |
          pacman -Syu --noconfirm
          pacman -S --noconfirm \
            meson \
            gcc \
            pkgconf \
            gtkmm-4.0 \
            glibmm-2.68 \
            libadwaita \
            git \
            tar
      - name: Compile
        run: |
          meson setup --prefix=/usr build
          meson compile -C build
          DESTDIR=$(pwd)/dist meson install -C build
          tar -czvf emojify-x86_64.tar.gz -C dist .
      - name: Upload Binary Artifact
        uses: actions/upload-artifact@v7
        with:
          name: arch-x86_64
          path: emojify-x86_64.tar.gz

  arch-binary-aarch64:
    name: "Build Arch Binary (aarch64)"
    needs: arch-binary-x86_64
    runs-on: self-hosted
    container:
      image: archlinux:base-devel
      options: --privileged
      volumes:
        - /tmp/emojify/pacman-cache-aarch64:/var/cache/pacman/pkg
    steps:
      - uses: actions/checkout@v6
      - name: Install Docker CLI
        run: |
          pacman -Syu --noconfirm
          pacman -S --noconfirm docker
      - name: Set up QEMU
        uses: docker/setup-qemu-action@v4
        with:
          platforms: arm64
      - name: Update System and Install Deps
        run: |
          pacman -Syu --noconfirm
          pacman -S --noconfirm \
            meson \
            gcc \
            pkgconf \
            gtkmm-4.0 \
            glibmm-2.68 \
            libadwaita \
            git \
            tar
      - name: Compile
        run: |
          meson setup --prefix=/usr build
          meson compile -C build
          DESTDIR=$(pwd)/dist meson install -C build
          tar -czvf emojify-aarch64.tar.gz -C dist .
      - name: Upload Binary Artifact
        uses: actions/upload-artifact@v7
        with:
          name: arch-aarch64
          path: emojify-aarch64.tar.gz
  appimage:
    name: "Build AppImage (Ubuntu 24.04)"
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6

      - name: Install Build Deps
        run: |
          sudo apt-get update
          sudo apt-get install -y meson g++ pkg-config libgtkmm-4.0-dev libadwaita-1-dev \
          desktop-file-utils appstream squashfs-tools fakeroot file gnupg zsync

      - name: Build and Install to AppDir
        run: |
          meson setup --prefix=/usr build
          meson compile -C build
          DESTDIR=$(pwd)/AppDir meson install -C build

      - name: Install appimage-builder
        run: |
          pip3 install appimage-builder --break-system-packages

      - name: Build AppImage
        run: appimage-builder --recipe AppImageBuilder.yml

      - name: Upload Artifact
        uses: actions/upload-artifact@v7
        with:
          name: Emojify-AppImage
          path: "*.AppImage*"
  publish:
    name: Publish Release
    needs:
      - arch-binary-x86_64
      - arch-binary-aarch64
      - appimage
    runs-on: self-hosted
    steps:
      - name: Download Artifacts
        uses: actions/download-artifact@v8
        with:
          path: ./assets

      - name: Prepare Release Assets
        run: |
          mkdir -p release_dist
          find ./assets -name "*.tar.gz" -exec cp {} ./release_dist/ \;
          find ./assets -name "*.AppImage" -exec cp {} ./release_dist/ \;
          cd release_dist
          sha256sum * > checksums.txt
      - name: Create Release
        uses: softprops/action-gh-release@v3
        with:
          generate_release_notes: true
          draft: true
          prerelease: false
          files: release_dist/*
